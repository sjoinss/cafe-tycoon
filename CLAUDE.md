# CLAUDE.md

이 저장소에서 작업하는 Claude Code를 위한 안내입니다.

## 프로젝트 개요

바닐라 HTML/CSS/JavaScript로 만든 카페 경영 시뮬레이션 게임(한국어 UI)입니다. 빌드 도구, 패키지 매니저, 프레임워크가 전혀 없습니다 — `index.html`을 정적으로 서빙하기만 하면 됩니다.

## 파일 구조

- `index.html` — DOM 마크업만 포함 (오버레이: 타이틀/슬롯/설정/상점/하루정산/튜토리얼 등)
- `style.css` — 전체 스타일. `:root`의 CSS 변수(`--bg`, `--panel`, `--accent` 등)로 테마 시스템 구현
- `script.js` — 게임 로직 전체 (약 3200줄, 모듈 분리 없이 하나의 스크립트)

세 파일은 원래 하나의 HTML 파일이었던 것을 분리한 것입니다. `<style>`/`<script>` 태그 안 내용을 그대로 옮긴 것이라 script.js 안에서 `document.getElementById` 등으로 index.html의 DOM을 직접 참조합니다.

## script.js 섹션 맵

파일 안에 `// ============ 섹션명 ============` 주석으로 구획이 나뉘어 있습니다. 특정 기능을 찾을 때 아래를 참고해 grep하면 빠릅니다.

- 기본 세팅 / 게임 모드 / 테마 시스템
- 가게 이름·시작화면 아이콘 커스텀
- 커스텀 캐릭터 이미지 / 손님 커스텀 / 바닥·가구 커스텀
- 이미지 드롭존 UI 헬퍼, 라디오/체크박스 칩 폴백
- 평판 시스템 / 팔레트 / 메뉴 정의 / 주방 설비 정의 / 알바 시스템
- 맵 & 배치 / 플레이어 / 모바일 카메라(확대/추적) + 세로 캔버스 / 방치형 모드 AI
- 상호작용 / 터치·모바일 조작 / 손님 시스템 / 미니게임(스테이션별 분기) / 상점
- 그리기(캔버스 렌더링) / 알바 캐릭터 그리기 / 대사
- 목표금액 달성(엔딩) 체크 / 하루 진행·정산
- 저장/불러오기 (IndexedDB, 슬롯 3개) / 전역 커스텀 저장(슬롯과 무관) / 내보내기·가져오기
- 설정창(캐릭터/인테리어/메뉴 커스텀 패널, 게임 관리) / 튜토리얼
- 모드 선택(새 게임 흐름) / 타이틀 화면 / 슬롯 선택 오버레이 / 모드 변경 확인
- 메인 루프 / 게임 시작 흐름

## 핵심 개념

- **게임 모드**: `gameMode`가 `'normal'`(직접 조작) 또는 `'idle'`(방치형)이며, `IDLE_BALANCE` 상수로 방치형 밸런스(스폰 간격, 가격, 인내심 등 배율)를 별도 관리합니다.
- **테마 시스템**: `THEMES` 객체(script.js)에 6개 팔레트(기본은 `white` = 크림색, 다크 포함)를 정의하고 `applyTheme()`에서 CSS 변수 및 캔버스용 `PAL`/`MG_UI` 객체에 동시 반영합니다. 밝은 테마들은 모두 카드(`panel`)가 흰색이고 배경 톤·포인트색만 다른 구조(토스류 앱과 동일한 방식)라, 새 테마를 추가할 땐 이 패턴을 따르는 게 일관적입니다. 다크 테마는 갈색이 아니라 남보라 계열로 만들어져 있으니(사용자가 "칙칙한 갈색" 피드백을 준 이력이 있음) 어두운 톤을 다시 갈색 계열로 되돌리지 마세요. 폰트는 Pretendard(CDN, index.html의 `<link>`)로 통일되어 있고, 과거 Jua/Trebuchet MS는 제거되었습니다 — 다시 추가하지 마세요.
  - `--accentOnDark` 등 `*OnDark` 변수는 캔버스 위에 항상 고정된 어두운 반투명 배경으로 뜨는 요소(대사창, 터치 조작 버튼, 스테이션 메뉴 팝업)에만 씁니다. 설정/상점/타이틀 화면처럼 전체 화면을 차지하는 오버레이는 이제 배경 자체가 테마를 따라가므로(`var(--bg)`) 그 안의 글자는 일반 `--accent`/`--text` 등을 씁니다. 새 오버레이를 만들 때 이 둘을 헷갈리면 밝은 테마에서 글자가 안 보이는 버그가 재발합니다.
- **커스터마이징**: 캐릭터(player/cook/server)·손님·바닥·가구·메뉴 아이콘 모두 사용자가 이미지를 업로드해 교체할 수 있습니다. `collectCustomizationBundle()`/`applyCustomizationBundle()`이 이 커스텀 전체(캐릭터/손님/메뉴/인테리어)를 직렬화/역직렬화하는 공용 헬퍼이고, 저장 슬롯 저장·전역 커스텀 저장·내보내기 파일이 모두 이 함수를 공유합니다.
- **저장 데이터**: 테마/가게 이름·아이콘은 `localStorage`(`cafeTycoonGlobalSettings_v1`)에, 게임 진행 슬롯(3개)은 IndexedDB(`cafeTycoonDB`)에 저장됩니다. 캐릭터/손님/메뉴/인테리어 커스텀은 각 슬롯에도 스냅샷으로 저장되지만, **슬롯 저장 여부와 무관하게** 같은 IndexedDB 저장소에 `slot:'globalCustom'`이라는 별도 레코드로도 항상 저장됩니다(`saveGlobalCustom()`/`loadGlobalCustom()`, 설정창을 닫을 때 자동 호출) — 슬롯을 하나도 저장하지 않아도 커스텀은 새로고침 후에도 남아있어야 하기 때문입니다. 설정 → 게임 관리 탭에 슬롯별 내보내기/가져오기(JSON 다운로드/업로드)와, 저장 진행도 없이 커스텀만 옮기는 별도의 "커스텀 내보내기/가져오기"가 있습니다.
- **모바일/터치 지원**: 캔버스를 탭하면 그 위치로 이동하고, 설비/테이블을 직접 탭하면 자동으로 걸어가서 도착 즉시 상호작용까지 이어집니다(`findTappedInteractable`, `pendingAutoInteract`). 별도의 "행동" 버튼은 사용자 요청으로 제거되었으니 다시 추가하지 마세요 — 대사 넘기기/미니게임 입력도 전부 캔버스 탭(`canvas.addEventListener('pointerdown', ...)`)으로 처리됩니다. 방향 입력이 필요한 디저트 포장 미니게임만 `#touchDirPad` 버튼으로 보완합니다.
  - **카메라/세로 캔버스**: 좁고 세로로 긴 화면에서는 `camera` 객체가 확대(zoom=1.7)와 추적을 담당하고, `resizeCanvasForViewport()`가 캔버스의 **세로 해상도 자체**를 늘려(가로는 800 고정) 확대율은 그대로 둔 채 창(프레임) 자체가 세로로 길어지게 합니다. 캔버스 해상도가 더 이상 항상 800×520이 아니므로, 화면 전체를 대상으로 한 캔버스 그리기(예: 미니게임 UI 박스)는 하드코딩된 800/520 대신 `canvas.width`/`canvas.height`를 참조해야 합니다.

## 작업 시 유의사항

- 세 파일 중 하나만 고쳐도 되는 경우가 많습니다 — 마크업/구조 변경은 `index.html`, 색상/레이아웃은 `style.css`, 게임 동작은 `script.js`.
- `script.js`는 파일 하나에 전역 상태와 함수가 모두 있는 구조입니다(모듈 시스템 없음). 새 기능을 추가할 때도 기존 섹션 주석 스타일(`// ============ 이름 ============`)을 따라 위치를 명확히 구분해주세요.
- 브라우저에서 직접 확인할 때 `file://`로 열면 일부 기능(특히 IndexedDB/모듈 관련)이 제한될 수 있으니 간단한 정적 서버로 띄워서 테스트하는 것을 권장합니다.

## Git / 커밋 계정 주의사항

- **`sonjiyo` 계정으로는 절대 커밋·푸시하지 않는다.** 이 저장소의 원격(origin)은 `sjoinss/cafe-tycoon.git`이며, 커밋/푸시 전에는 항상 다음을 확인한다:
  - `git config --local user.name` / `user.email` (현재 `Claude` / `tmhww4@gmail.com`로 설정되어 있음 — `claude-code@anthropic.com`은 Claude Code 사용자 공용 이메일이라 다른 GitHub 계정이 그 이메일을 등록해두면 커밋이 그 계정 것으로 잘못 표시될 수 있어 변경함)
  - `gh auth status` — 활성 계정이 `sjoinss`인지 확인 (sonjiyo 계정이 로그인되어 있거나 활성화되어 있으면 즉시 중단하고 사용자에게 알린다)
  - Windows 자격 증명 관리자(`cmdkey /list`)에 sonjiyo 관련 GitHub 자격 증명이 새로 추가되었는지도 확인
- 위 확인 없이 `git push`를 실행하지 않는다.
